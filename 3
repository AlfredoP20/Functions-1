#include<stdio.h>
#include<conio.h>

int main()
{
    char "X" [31];
    printf("Ingrese un número:");
    gets(X);
    int cant=0;
    while (X[cant]!='\0')
    {
        cant++;
    }; return 0;
}
